#include <iostream>

int main () {

    int grade = 75;
    
    grade >= 60 ? std::cout << "you pass!" : std::cout << "you fail! ";

    return 0;
}

// it is used instead of else if statements
// int can be used in the form of critera ? the thing to if its true : the you wnt to do if its false
